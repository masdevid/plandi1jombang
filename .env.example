# API Configuration
API_URL=http://localhost:3000/api

# Database (SQLite - file-based, no credentials needed)
# The database file will be created at: data/attendance.db

# CORS Configuration (optional - defaults to *)
# ALLOWED_ORIGINS=http://localhost:4200,https://your-domain.vercel.app
